<h2> All Users </h2>
<% if @users.empty? %>
    <p> There are no users </p>
<% else%>
    <ul>
        <% @users.each do |user| %>
            <li> <%= link_to user.name, user %> of category
                 <%= link_to user.category.name, user.category %>.
            </li> 

        <% end %>
    </ul>
<% end %>