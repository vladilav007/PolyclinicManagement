<h2> All Doctors </h2>
<% if @doctors.empty? %>
    <p> There are no doctors </p>
<% else%>
    <ul>
        <% @doctors.each do |doctor| %>
            <li> 
                 <%= link_to doctor.name, doctor %> of category
                 <%= link_to doctor.category.name, doctor.category %>.
            </li> 

        <% end %>
    </ul>
<% end %>